# Composer Related
composer.lock
/vendor

# Frontend Assets
/node_modules

# Logs
npm-debug.log
yarn-error.log

# Caches
.phpunit.cache
.phpunit.result.cache
/build

# IDE Helper
_ide_helper.php
_ide_helper_models.php
.phpstorm.meta.php

# Editors
/.idea
/.fleet
/.vscode

# Misc
# `testbench.yaml` is deliberately not ignored — it is what wires up the
# workbench demo app, so it belongs in the repository.
phpunit.xml
phpstan.neon
/docs
/coverage

# Workbench
/workbench/storage
/.playwright-mcp
